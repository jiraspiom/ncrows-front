/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cache.pnix.exchange',
      'gcdn.wemade.games',
      'cache.wemixplay.com',
    ],
  },
}

export default nextConfig
